# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Encryption key for MCP credentials (generate with: openssl rand -hex 32)
KEY_ENCRYPTION_SECRET=

# vLLM Local LLM Endpoint
VLLM_BASE_URL=http://your-nlb-url/v1

# Anthropic API Key (optional — for Claude models)
# ANTHROPIC_API_KEY=
